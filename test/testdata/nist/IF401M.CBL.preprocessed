      *> HEADER,COBOL,IF401M
       IDENTIFICATION DIVISION.
       PROGRAM-ID.
            IF401M.
       
       
      *> THIS PROGRAM TESTS THE FLAGGING OF HIGH SUBSET INTRINSIC FUNCTION
      *> FEATURES.
      *> *****************************************************************
      *>     THIS PROGRAMS CONTAINS TESTS FOR THE FOLLOWING INTRINSIC    *
      *>     FUNCTIONS:  ACOS, ANNUITY, ASIN, ATAN, CHAR, COS,           *
      *>                 CURRENT-DATE, DATE-OF-INTEGER, DAY-OF-INTEGER,  *
      *>                 FACTORIAL, INTEGER, INTEGER-OF-DATE,            *
      *>                 INTEGER-OF-DAY AND INTEGER-PART.                *
      *> *****************************************************************
       
       ENVIRONMENT DIVISION.
       CONFIGURATION SECTION.
       SOURCE-COMPUTER.
           XXXXX082.
       OBJECT-COMPUTER.
           XXXXX083.
       
       DATA DIVISION.
       FILE SECTION.
       WORKING-STORAGE SECTION.
       01  TEMP1        PICTURE    X(21).
       01  WS-ANUM      PICTURE    X.
       
       PROCEDURE DIVISION.
       IF401M-ACOS.
           IF FUNCTION ACOS (1.0) = FUNCTION ACOS (1.0)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF401M-ANNUITY.
           IF FUNCTION ANNUITY (0, 4) = FUNCTION ANNUITY (0, 4)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF401M-ASIN.
           IF FUNCTION ASIN (1.0) = FUNCTION ASIN (1.0)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF401M-ATAN.
           IF FUNCTION ATAN (1.0) = FUNCTION ATAN (1.0)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF401M-CHAR.
           MOVE FUNCTION CHAR (37) TO WS-ANUM.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF401M-COS.
           IF FUNCTION COS (1.0) = FUNCTION COS (1.0)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF401M-CURRENT-DATE.
           MOVE FUNCTION CURRENT-DATE TO TEMP1.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF401M-DATE-OF-INTEGER.
           IF FUNCTION DATE-OF-INTEGER (1) =
              FUNCTION DATE-OF-INTEGER (1)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF401M-DAY-OF-INTEGER.
           IF FUNCTION DAY-OF-INTEGER (1) = FUNCTION DAY-OF-INTEGER (1)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF401M-FACTORIAL.
           IF FUNCTION FACTORIAL (1) = FUNCTION FACTORIAL (1)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF401M-INTEGER.
           IF FUNCTION INTEGER (1.0) = FUNCTION INTEGER (1.0)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF401M-INTEGER-OF-DATE.
           IF FUNCTION INTEGER-OF-DATE (16010101) =
              FUNCTION INTEGER-OF-DATE (16010101)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF401M-INTEGER-OF-DAY.
           IF FUNCTION INTEGER-OF-DAY (1601001) =
              FUNCTION INTEGER-OF-DAY (1601001)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF401M-INTEGER-PART.
           IF FUNCTION INTEGER-PART (4.578) =
              FUNCTION INTEGER-PART (4.578)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF401M-END.
       
      *> TOTAL NUMBER OF FLAGS EXPECTED = 14.
      *> END-OF,IF401M