      *> HEADER,COBOL,IF403M
       IDENTIFICATION DIVISION.
       PROGRAM-ID.
            IF403M.
       
       
      *> THIS PROGRAM TESTS THE FLAGGING OF HIGH SUBSET INTRINSIC FUNCTION
      *> FEATURES.
      *> *****************************************************************
      *>     THIS PROGRAMS CONTAINS TESTS FOR THE FOLLOWING INTRINSIC    *
      *>     FUNCTIONS:  PRESENT-VALUE, RANDOM, RANGE, REM, REVERSE,     *
      *>                 SIN, SQRT, STANDARD-DEVIATION, SUM, TAN,        *
      *>                 UPPER-CASE, VARIANCE AND WHEN-COMPILED.         *
      *> *****************************************************************
       
       ENVIRONMENT DIVISION.
       CONFIGURATION SECTION.
       SOURCE-COMPUTER.
           XXXXX082.
       OBJECT-COMPUTER.
           XXXXX083.
       
       DATA DIVISION.
       FILE SECTION.
       WORKING-STORAGE SECTION.
       01  WS-AN-TEMP   PICTURE    X(21).
       
       PROCEDURE DIVISION.
       IF403M-PRESENT-VALUE.
           IF FUNCTION PRESENT-VALUE (0, 23, 12, 9) =
              FUNCTION PRESENT-VALUE (0, 23, 12, 9)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF403M-RANDOM.
           IF FUNCTION RANDOM (1) = FUNCTION RANDOM (1)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF403M-RANGE.
           IF FUNCTION RANGE (5, -2, -14, 0) =
              FUNCTION RANGE (5, -2, -14, 0)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF403M-REM.
           IF FUNCTION REM (0, 20) = FUNCTION REM (0, 20)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF403M-REVERSE.
           MOVE FUNCTION REVERSE ("ABC") TO WS-AN-TEMP.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF403M-SIN.
           IF FUNCTION SIN (1.0) = FUNCTION SIN (1.0)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF403M-SQRT.
           IF FUNCTION SQRT (0) = FUNCTION SQRT (0)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF403M-STANDARD-DEVIATION.
           IF FUNCTION STANDARD-DEVIATION (5, -2, -14, 0) =
              FUNCTION STANDARD-DEVIATION (5, -2, -14, 0)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF403M-SUM.
           IF FUNCTION SUM (5, -2, -14, 0) =
              FUNCTION SUM (5, -2, -14, 0)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF403M-TAN.
           IF FUNCTION TAN (1.0) = FUNCTION TAN (1.0)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF403M-UPPER-CASE.
           MOVE FUNCTION UPPER-CASE ("abc") TO WS-AN-TEMP.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF403M-VARIANCE.
           IF FUNCTION VARIANCE (5, -2, -14, 0) =
              FUNCTION VARIANCE (5, -2, -14, 0)
                       CONTINUE.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF403M-WHEN-COMPILED.
           MOVE FUNCTION WHEN-COMPILED TO WS-AN-TEMP.
      *> MESSAGE EXPECTED FOR ABOVE STATEMENT: NON-CONFORMING STANDARD
       
       IF403M-END.
       
      *> TOTAL NUMBER OF FLAGS EXPECTED = 13.
      *> END-OF,IF403M